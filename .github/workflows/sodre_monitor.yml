name: Sodré Santoro Monitor

on:
  schedule:
    # Executa a cada 4 horas
    - cron: '0 */4 * * *'
  
  # Permite execução manual
  workflow_dispatch:

env:
  PYTHONUNBUFFERED: '1'  # Logs em tempo real

jobs:
  monitor-sodre:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 🔵 Checkout código
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Instalar dependências
        run: |
          pip install --upgrade pip
          pip install supabase playwright beautifulsoup4
      
      - name: 🎭 Instalar browsers do Playwright
        run: |
          playwright install --with-deps chromium
      
      - name: 🚀 Executar Monitor Sodré Santoro
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          echo "════════════════════════════════════════════════════════════════════"
          echo "🔵 INICIANDO MONITOR SODRÉ SANTORO"
          echo "⏰ $(date '+%Y-%m-%d %H:%M:%S UTC')"
          echo "════════════════════════════════════════════════════════════════════"
          python scraper/sodre_monitor.py
      
      - name: ✅ Verificar resultado
        if: failure()
        run: echo "❌ Monitor Sodré Santoro falhou!"